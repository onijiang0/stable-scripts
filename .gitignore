# 工作区元数据（不应入库）
.workbuddy/

# ── 文档同步规则 ──────────────────────────────────────────
# 只同步「关于脚本」的文档（README / archive 的 NOTE 等）。
# 不同步「关于本地运行」的文档（工作区结构、Agent 规则、内网对接口径）。
# 详见 AGENTS.md §8.5 知识归位。
AGENTS.md

# 失效/无法运行的链路：仅保留本地，不入库
# 翼支付 MGS 全链路（yzf_sign.py -> yzf_mgs_client.js -> yzf_js/），实测已失效
ql/yzf_js/
ql/yzf_mgs_client.js
ql/yzf_sign.py

# 凭据 / 登录态缓存（青龙脚本运行时会生成）
.env
*.env
auth.json
*.token
*.cookie

# 本地配置
config.local.*
*.local

# 运行产物
__pycache__/
*.pyc
node_modules/
*.log
.DS_Store
Thumbs.db
